# ASCII码表  常用的字符串都可以在表中找到对应的码值  码值大的就大
# 字符串是按位比较，也就是一位一位进行比价，只要有一位打，那么整体就大

print(f"abd大于abc，结果：{'abd' > 'abc'}")

# 比较a和ab
print(f"ab大于a，结果：{'ab' > 'a'}")
# 比较a和A
print(f"a大于A，结果：{'a' > 'A'}")
# 比较key2和key1
print(f"key2大于key1，结果：{'key2' > 'key1'}")  # 前面的key是一样的，就是更具最后的2做比较

